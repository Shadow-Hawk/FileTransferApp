# TCP port of receiver
DestPort=9999

# UDP port of sender
SrcUdpPort=5555

# Sender's IP
SrcHost=192.168.10.2

# Receiver's IP
DestHost=192.168.10.3

# Files hosted on sender (server)
SrcDir=/home/acn-servertest/Desktop/ServerFiles

# Receiver's folder (client)
DestDir=/home/acn-clienttest/Desktop/ClientFiles

ThreadCount=2

# Disk buffer in KB
BufferSize=12400

# KB
SocketBufferSize=62

ZipFile=thousands_of_tiny_files

# Stream I/O : 0
# transferTo/transferFrom: 1
HandlerMode=0

# MaxFileSize in MB: if one file exceeds this size, it will be split
MaxFileSize = 20480000

# Use TCP
UseTcpForCommand=true
